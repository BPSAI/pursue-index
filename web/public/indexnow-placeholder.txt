PLACEHOLDER — REPLACE WITH REAL INDEXNOW KEY FILE.

This file is a stub for Sprint 4b Theme B. The post-deploy IndexNow
workflow (.github/workflows/indexnow-after-deploy.yml) is wired up but
will exit 0 with "no key found" until the operator does:

  1. Generate a random hex key:
       $ python3 -c "import secrets; print(secrets.token_hex(16))"

  2. Save THAT exact key string in a file at:
       web/public/<key>.txt

     (One line containing only the key. No quotes, no key=value
     prefix, no trailing comment.)

  3. Add the same value as a GitHub Actions secret named
     INDEXNOW_KEY under repo Settings → Secrets and variables →
     Actions.

  4. Delete this placeholder file.

After the next deploy, https://pursueindex.com/<key>.txt serves the
key, IndexNow verifies ownership, and the workflow's post-deploy ping
starts populating Bing / Yandex / ChatGPT-search within minutes of
each tranche update.

See docs at https://www.indexnow.org/documentation for the protocol.
