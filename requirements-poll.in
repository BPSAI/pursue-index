# Source for requirements-poll.txt (regenerate with:
#   pip-compile --generate-hashes -o requirements-poll.txt requirements-poll.in
# ).
#
# Pinned dependencies for the lightweight poll workflow
# (.github/workflows/poll-pursue.yml). Keep this list as small as
# possible — we install on every cron run, so each addition slows
# poll latency and broadens supply-chain surface.
curl_cffi>=0.7
pydantic>=2.0
structlog>=24.0
pydantic-settings>=2.0
